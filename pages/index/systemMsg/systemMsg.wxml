<!--pages/index/systemMsg/systemMsg.wxml-->
<view class="container">
    <view class="msg-item"wx:for="{{data}}"wx:key="{{index}}">
        <view class="msg-item-title">{{item.title}}</view>
        <view class="msg-item-cnt">{{item.content}}</view>
    </view>
</view>
